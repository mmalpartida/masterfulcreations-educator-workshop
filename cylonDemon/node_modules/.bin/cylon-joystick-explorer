#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cylon-joystick/bin/cylon-joystick-explorer" "$@"
  ret=$?
else 
  node  "$basedir/../cylon-joystick/bin/cylon-joystick-explorer" "$@"
  ret=$?
fi
exit $ret
